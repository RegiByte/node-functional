import { Event, ValidatedEvent } from '@marblejs/core';
import { Observable } from 'rxjs';
import { ValidatorOptions } from './io.middleware';
export declare const eventValidator$: <U extends import("io-ts").Any, T extends Event<unknown, any, string>>(schema: U, options?: ValidatorOptions | undefined) => (event$: Observable<T>) => Observable<ValidatedEvent<U["_A"], string>>;
