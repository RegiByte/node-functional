import * as t from 'io-ts';
import { Reporter } from 'io-ts/lib/Reporter';
import { Observable } from 'rxjs';
export declare type Schema = t.Any;
export interface ValidatorOptions {
    reporter?: Reporter<any>;
    context?: string;
}
export declare const validator$: <U extends t.Any, T>(schema: U | undefined, options?: ValidatorOptions) => (i$: Observable<T>) => Observable<U["_A"]>;
