import { Timestamp } from 'rxjs';
import { HttpRequest, HttpResponse } from '@marblejs/core';
import { LogParams } from './logger.model';
declare type LogFactorizerOptions = {
    colorize?: boolean;
    timestamp?: boolean;
};
export declare const prepareLogString: (opts: LogParams) => string;
export declare const factorizeLog: (res: HttpResponse, stamp: Timestamp<HttpRequest<unknown, unknown, unknown>>) => (opts?: LogFactorizerOptions) => string;
export {};
