import { Observable } from 'rxjs';
import { HttpRequest, HttpResponse } from '../http.interface';
import { EffectMetadata } from '../effects/effects.interface';
import { HttpEffectResponse } from '../effects/http-effects.interface';
import { RouteMatched, RoutingItem } from './router.interface';
export { RoutingItem };
export declare const findRoute: (routing: RoutingItem[], url: string, method: "POST" | "PUT" | "PATCH" | "GET" | "HEAD" | "DELETE" | "CONNECT" | "OPTIONS" | "TRACE" | "*") => RouteMatched | undefined;
export declare const resolveRouting: (routing: RoutingItem[], metadata: EffectMetadata<Error, any>) => (res: HttpResponse) => (req: HttpRequest<unknown, unknown, unknown>) => Observable<HttpEffectResponse<any>>;
