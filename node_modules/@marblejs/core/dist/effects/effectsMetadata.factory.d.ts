import { EffectMetadata } from './effects.interface';
import { SchedulerLike } from 'rxjs';
import { ContextProvider } from '../context/context.factory';
export declare const createEffectMetadata: <Scheduler extends SchedulerLike, Err extends Error, Initiator = any>(metadata: {
    ask: ContextProvider;
    scheduler?: Scheduler | undefined;
    error?: Err | undefined;
    initiator?: Initiator | undefined;
}) => EffectMetadata<Err, Initiator>;
