import { Observable } from 'rxjs';
import { Effect, EffectMetadata } from './effects.interface';
export declare const combineMiddlewares: <T, U>(...effects: Effect<T, T, U, Error, any>[]) => (input$: Observable<T>, client: U, meta: EffectMetadata<Error, any>) => Observable<T>;
export declare const combineEffects: <T, U, V>(...effects: Effect<T, U, V, Error, any>[]) => (input$: Observable<T>, client: V, meta: EffectMetadata<Error, any>) => Observable<U>;
