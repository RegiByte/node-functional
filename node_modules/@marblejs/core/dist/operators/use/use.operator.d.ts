import { Observable } from 'rxjs';
import { EffectMetadata } from '../../effects/effects.interface';
interface MiddlewareLike<I, O> {
    (i$: Observable<I>, client: any, meta: any): Observable<O>;
}
export declare const use: <I, O>(middleware: MiddlewareLike<I, O>, client?: any, meta?: EffectMetadata<Error, any> | undefined) => (source$: Observable<I>) => Observable<O>;
export {};
