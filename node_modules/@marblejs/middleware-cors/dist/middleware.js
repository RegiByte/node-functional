"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const core_1 = require("@marblejs/core");
const operators_1 = require("rxjs/operators");
const util_1 = require("util");
const configurePreflightResponse_1 = require("./configurePreflightResponse");
const configureResponse_1 = require("./configureResponse");
const DEFAULT_OPTIONS = {
    origin: '*',
    methods: ['HEAD', 'GET', 'POST', 'PUT', 'PATCH', 'DELETE', 'OPTIONS'],
    withCredentials: false,
    optionsSuccessStatus: core_1.HttpStatus.NO_CONTENT,
};
exports.cors$ = (options = {}) => (req$, res) => {
    options = Object.assign({}, DEFAULT_OPTIONS, options);
    return req$.pipe(operators_1.tap(req => {
        // skip if not a CORS request
        if (!util_1.isString(req.headers.origin)) {
            return;
        }
        if (req.method === 'OPTIONS') {
            configurePreflightResponse_1.configurePreflightResponse(req, res, options);
            res.end();
        }
        else {
            configureResponse_1.configureResponse(req, res, options);
        }
    }));
};
