"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const util_1 = require("./util");
exports.checkStringOrigin = (origin, option) => {
    if (util_1.isString(option) && option === '*') {
        return true;
    }
    else if (util_1.isString(option) &&
        option !== '*' &&
        origin.match(option)) {
        return true;
    }
    return false;
};
