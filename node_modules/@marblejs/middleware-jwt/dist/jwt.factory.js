"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) if (e.indexOf(p[i]) < 0)
            t[p[i]] = s[p[i]];
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
const rxjs_1 = require("rxjs");
const jwt = require("jsonwebtoken");
exports.generateExpirationInHours = (hours = 1) => Math.floor(Date.now() / 1000) + (60 * 60 * hours);
exports.generateToken = (_a) => {
    var { secret } = _a, opts = __rest(_a, ["secret"]);
    return (payload = {}) => jwt.sign(payload, secret, opts);
};
exports.verifyToken = (_a) => {
    var { secret } = _a, opts = __rest(_a, ["secret"]);
    return (token) => jwt.verify(token, secret, opts);
};
exports.verifyToken$ = (_a) => {
    var { secret } = _a, opts = __rest(_a, ["secret"]);
    return token => new rxjs_1.Observable(subscriber => {
        jwt.verify(token, secret, opts, (error, payload) => {
            if (error) {
                return subscriber.error(error);
            }
            subscriber.next(payload);
            subscriber.complete();
        });
    });
};
